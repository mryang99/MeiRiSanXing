name: MeiRiSanXing
options:
  bundleIdPrefix: com.example
  deploymentTarget:
    iOS: "18.0"
targets:
  MeiRiSanXing:
    type: application
    platform: iOS
    sources:
      - Sources/MeiRiSanXing
    info:
      path: Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.meirisanxing
        INFOPLIST_FILE: Info.plist
        CODE_SIGN_STYLE: Automatic
